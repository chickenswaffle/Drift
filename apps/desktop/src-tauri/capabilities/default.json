{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main DRIFT window (events + the rpc bridge).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-content-protected",
    "updater:default",
    "process:default",
    "notification:default"
  ]
}
